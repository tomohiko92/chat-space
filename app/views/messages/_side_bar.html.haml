.side_bar
 .side_bar__username
  .top-items
   = current_user.name
   .top-items__icons
    =link_to "#", class:"icon-a" do
     =icon('far','edit')
    =link_to edit_user_path(current_user), class:"icon-b" do
     =icon('fas','cog')
 .side_bar__grouplist
  .groups
   .groups__groupname
    techcamp
   .groups__groupmention
    まだメッセージがありません
  
  .groups
   .groups__groupname
    techcamp
   .groups__groupmention
    まだメッセージがありません